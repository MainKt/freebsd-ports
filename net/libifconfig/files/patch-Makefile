--- Makefile.orig	2025-06-21 02:57:45 UTC
+++ Makefile
@@ -1,15 +1,12 @@ LIB=		ifconfig
 PACKAGE=	lib${LIB}
 LIB=		ifconfig
-INTERNALLIB=	true
 
-LIBADD=		m
+LDADD+=		-lm
 
-SHLIBDIR?=	/lib
+LIBDIR=	${PREFIX}/lib
+SHLIBDIR=	${PREFIX}/lib
 SHLIB_MAJOR=	2
 
-VERSION_DEF=	${LIBCSRCDIR}/Versions.def
-SYMBOL_MAPS=	${.CURDIR}/Symbol.map
-
 SRCS=		libifconfig.c \
 		libifconfig_bridge.c \
 		libifconfig_carp.c \
@@ -26,7 +23,7 @@ SRCS+=	${GEN}
 
 SRCS+=	${GEN}
 
-.include <src.lua.mk>
+LUA=	${LIBEXECDIR}/flua
 
 .SUFFIXES: .tpl.c .tpl.h
 .tpl.c.c .tpl.h.h: sfp.lua
@@ -35,8 +32,8 @@ CLEANFILES+= ${GEN}
 CLEANFILES+= ${GEN}
 
 # If libifconfig become public uncomment those two lines
-#INCSDIR=	${INCLUDEDIR}
-#INCS=		libifconfig.h libifconfig_sfp.h libifconfig_sfp_tables.h
+INCSDIR=	${PREFIX}/include
+INCS=		libifconfig.h libifconfig_sfp.h libifconfig_sfp_tables.h
 
 #MAN=		libifconfig.3
 
